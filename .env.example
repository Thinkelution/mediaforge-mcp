# === Required for V1 ===
GROQ_API_KEY=gsk_xxxxxxxxxxxxx           # Free at console.groq.com

# === Required for V2 ===
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxx     # For image gen, TTS
HUGGINGFACE_API_KEY=hf_xxxxxxxxxxxxx     # Optional fallback

# === Server Config ===
MEDIAFORGE_TRANSPORT=http                # stdio | http
MEDIAFORGE_PORT=3100                     # HTTP server port (when transport=http)
MEDIAFORGE_TEMP_DIR=/tmp/mediaforge      # Working directory for temp files
MEDIAFORGE_MAX_FILE_SIZE_MB=500          # Max input file size
MEDIAFORGE_CLEANUP_AFTER_HOURS=2         # Auto-delete temp files after N hours
MEDIAFORGE_LOG_LEVEL=info                # debug | info | warn | error

# === Provider Preferences ===
TRANSCRIPTION_PROVIDER=groq              # groq | replicate | huggingface
IMAGE_GEN_PROVIDER=replicate             # replicate | huggingface
TTS_PROVIDER=replicate                   # replicate | huggingface
